# AI 编码规范

> 本文档面向所有参与 emu-chat 开发的 AI 编码助手。在编写或修改代码之前，必须阅读并遵循以下规范。

---

## 1. 项目阶段：前期开发，无需向前兼容

emu-chat 当前处于**前期开发阶段**，尚未在任何生产环境中部署。

- **不需要向前兼容**：数据库 schema 变更可以直接修改 `0001_initial.sql`，不需要编写增量迁移脚本。
- **开发数据可随时丢弃**：开发过程中产生的 SQLite 数据库、草稿、队列等数据都可以直接删除重建。
- **接口可自由调整**：REST API、Zod Schema、TypeScript 类型均可按需重构，不需要考虑旧版本客户端兼容。

## 2. 测试策略：只保留必要测试

- **严禁过度测试**：本项目不追求测试覆盖率（Code Coverage）指标，严禁为了指标编写无意义的测试。
- **单元测试（Unit Tests）**：
  - **允许且必要**：复杂状态机转换逻辑（Queue 9 态、Run 6 态）、并发排他租约（Lease）获取与续约、CAS 乐观锁版本冲突校验、数据一致性校验。
  - **禁止编写**：简单的 CRUD、普通 getter/setter、直白的纯函数工具、基础 Map/LRU 缓存读写等一眼可见或无风险的代码。
- **集成测试（Integration Tests）**：
  - **仅保留主干**：只保留核心消息流转链路（消息入队 → 协调器调度 → 上游 Hermes 交互/SSE 消费 → 状态对账完成）以及关键的异常恢复机制（进程重启/断线恢复）。删除重复或细枝末节的矩阵组合测试。
- **组件测试（Component Tests）**：
  - **禁止测试纯静态渲染**：严禁测试组件是否渲染了某段文字、占位符（placeholder）、图标或普通按钮（肉眼即可验证，测试代码极其脆弱）。
  - **仅保留高风险核心交互测试**：
    1. **不可逆删除防护**：删除确认弹窗的防误触逻辑（必须手动勾选确认复选框后才能触发真正删除）；
    2. **数据防丢失保护**：网络断网或发送失败时，输入框内草稿内容必须保留，严禁丢失用户输入；
    3. **异步竞态安全**：用户正在输入的新内容不被旧异步保存回调的快照覆盖。
- **重构与测试维护原则**：
  - 需要写新的测试时，如果不确定是否必要，先咨询用户的意见。
  - 遇到业务改动导致现有测试失败时，**优先判断测试是否属于过度测试或已过时**。
  - 对于已过时或非核心测试，**坚决直接删除**，严禁为了让测试通过而扭曲业务实现代码。

