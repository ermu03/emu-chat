export const LIMITS = {
  // Input & Draft
  INPUT_MAX_BYTES: 64 * 1024, // 64 KiB UTF-8
  JSON_BODY_MAX_BYTES: 128 * 1024, // 128 KiB
  TITLE_MAX_CHARS: 200,

  // Tags
  TAG_MAX_CHARS: 40,
  TAGS_MAX_COUNT: 20,

  // Queue
  QUEUE_ACTIVE_MAX_COUNT: 100, // Non-terminal items per conversation

  // Pagination
  SESSION_LIST_PAGE_DEFAULT: 50,
  SESSION_LIST_PAGE_MAX: 100,
  MESSAGES_PAGE_DEFAULT: 100,
  MESSAGES_PAGE_MAX: 200,

  // Concurrency & Leases
  MAX_ACTIVE_GLOBAL_RUNS: 1,
  MAX_ACTIVE_CONVERSATION_RUNS: 1,
  LEASE_TTL_MS: 15_000,
  LEASE_HEARTBEAT_INTERVAL_MS: 5_000,

  // Admission & Retries
  MAX_ADMISSION_ATTEMPTS: 4,
  IDEMPOTENCY_WINDOW_SECONDS: 86_400,

  // Recovery & TTLs
  RECOVERY_RETENTION_DAYS: 7,
  RECOVERY_RETENTION_MS: 7 * 24 * 60 * 60 * 1000,
  TERMINAL_CONTROL_RETENTION_DAYS: 7,
  TERMINAL_CONTROL_RETENTION_MS: 7 * 24 * 60 * 60 * 1000,
  DISCARDED_CONTROL_RETENTION_DAYS: 30,
  DISCARDED_CONTROL_RETENTION_MS: 30 * 24 * 60 * 60 * 1000,

  // SSE & Ring buffer
  SSE_MAX_CONNS_PER_RUN: 5,
  SSE_MAX_CONNS_GLOBAL: 20,
  EVENT_RING_MAX_EVENTS: 512,
  EVENT_RING_MAX_BYTES: 1024 * 1024, // 1 MiB UTF-8
  SSE_HEARTBEAT_INTERVAL_MS: 15_000,

  // Upstream timeouts
  UPSTREAM_READ_TIMEOUT_MS: 10_000,
  UPSTREAM_MUTATION_TIMEOUT_MS: 10_000,
  UPSTREAM_RUN_SUBMIT_TIMEOUT_MS: 30_000,
  UPSTREAM_SSE_HANDSHAKE_TIMEOUT_MS: 10_000,
  UPSTREAM_SSE_LIVENESS_TIMEOUT_MS: 30_000,
  UPSTREAM_MAX_RESPONSE_BYTES: 4 * 1024 * 1024, // 4 MiB non-SSE
  UPSTREAM_MAX_SSE_FRAME_BYTES: 256 * 1024, // 256 KiB single frame

  // Reconciliation
  RECONCILIATION_MAX_DURATION_MS: 5 * 60 * 1000, // 5 minutes

  // UI preferences
  SIDEBAR_WIDTH_MIN: 240,
  SIDEBAR_WIDTH_MAX: 520,
  SIDEBAR_WIDTH_DEFAULT: 320
} as const;
